name: ydental_application
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  font_awesome_flutter:
  flutter_svg:
  intl:
  iconsax:
  provider:
  flutter_rating_bar:
  url_launcher:
  image_picker:
  shared_preferences:
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/Search.svg
    - assets/Filter.svg
    - assets/cases/
    - assets/img/
